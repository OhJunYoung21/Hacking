### 파일 생성(touch)

리눅스에서는 여러가지 명령어를 사용해서 파일을 생성할 수 있다. 파일 말고 디렉토리를 생성할때는 mkdir를 사용하는데
이건 디렉토리니까 일단 넘어가고..

~~~
touch [파일 이름]
~~~

### 파일 읽기 및 쓰기(cat,head,tail)

touch는 파일을 생성할 수는 있으나, 파일안에 내용을 읽어오거나 쓸 수는 없다. 사람의 관점으로 봐서는 얼탱이가 없는 일이 아닐 수 없는데, '이는 아 내가 노트를 만들었는데 그 안에 뭐가 있는지 볼수도 없고 그 안에 뭘 쓸수도 없어' 이런 느낌인 것이다.

일반적으로 파일에 내용을 쓸 때에는 cat과 vim과 같은 텍스트 편집기를 사용하며, 필자는 연구실에서 bash(.sh)파일을 편집할때 주로 vim을 자주 사용하고는 했었다.
